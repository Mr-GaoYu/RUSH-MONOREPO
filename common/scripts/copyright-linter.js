const fs = require("fs");
const path = require("path");
const child_process = require("child_process");

console.log(process.argv, 112321)

const filePaths = process.argv.reduce((acc, cur) => {

}, false)

if (filePaths) {

}